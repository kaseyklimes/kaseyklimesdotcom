// Shared with cache warming so delivery settings and warmed variants agree.
export const imageDeviceSizes = [640, 750, 828, 1080, 1200, 1920, 2048, 2560, 3840];
export const imageFormats = ['image/avif', 'image/webp'];
